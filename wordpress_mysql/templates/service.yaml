apiVersion: v1
kind: Service
metadata:
  name: {{ include "wordpress.fullname" . }}
  labels:
    {{- include "wordpress.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: wordpress-mysql
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
