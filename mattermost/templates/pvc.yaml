apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mattermost.fullname" . }}-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.storage.size }}
  storageClassName: local-path
