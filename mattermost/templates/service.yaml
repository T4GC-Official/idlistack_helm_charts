apiVersion: v1
kind: Service
metadata:
  name: {{ include "mattermost.fullname" . }}-service
spec:
  type: ClusterIP
  selector:
    app: {{ include "mattermost.fullname" . }}
  ports:
    - name: http
      port: 80
      targetPort: 8065
