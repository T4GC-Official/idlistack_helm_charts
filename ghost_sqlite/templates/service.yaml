apiVersion: v1
kind: Service
metadata:
  name: {{ include "ghost.fullname" . }}-service
  labels:
    {{- include "ghost.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ghost.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.ghost.port }}
      protocol: TCP
